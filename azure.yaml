# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: containerapp-appgw-demo
metadata:
  template: containerapp-appgw-demo@0.0.1-beta

infra:
  provider: bicep

services:
  api:
    project: .
    language: python
    host: containerapp
    docker:
      path: Dockerfile
      context: .